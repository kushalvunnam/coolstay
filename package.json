{
  "name": "coolstay-monorepo",
  "version": "1.0.0",
  "description": "Monorepo root for CoolStay PG/Hostel Management System",
  "scripts": {
    "install-client": "cd client && npm install --legacy-peer-deps",
    "install-server": "cd server && npm install --legacy-peer-deps",
    "install-all": "npm install --legacy-peer-deps && npm run install-client && npm run install-server",
    "build-client": "cd client && npm run build",
    "start": "cd server && node server.js",
    "build": "npm run install-client && npm run build-client && npm run install-server"
  }
}
